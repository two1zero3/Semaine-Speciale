//import svg 
PShape M_Path;

void loadSVGs() {
    // M_Path = loadShape("data/game/M_Path.svg");
}

void lineGameTest(float x, float y, int scaleSize, color c) {
//     //make an M letter with lines
    // push();
       
    //     shape(M_Path, x, y);
    
    // pop();
    
    // if (M_Path.contains(activeItem.position.x, activeItem.position.y)) {
    //     circle(200,200,50);
    // }
}